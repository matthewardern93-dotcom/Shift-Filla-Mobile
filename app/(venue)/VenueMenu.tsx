
import { Text, View } from 'react-native';
import VenueScreenTemplate from '../../components/templates/VenueScreenTemplate';

const VenueMenuScreen = () => {
  return (
    <VenueScreenTemplate>
      <View>
        <Text>Venue Menu Screen</Text>
      </View>
    </VenueScreenTemplate>
  );
};

export default VenueMenuScreen;
