export enum JobStatus {
    OPEN = 'open',
    INTERVIEWING = 'interviewing',
    OFFERED = 'offered',
    FILLED = 'filled',
    CLOSED = 'closed',
}

export enum ShiftStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    OFFERED = 'offered',
}